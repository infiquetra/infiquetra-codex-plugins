{
  "judgment": {
    "default_model": "opus",
    "default_effort": "high",
    "rationale": "Judgment, design, adversarial review, architectural decisions — deep reasoning needed; cost-justified."
  },
  "mechanical": {
    "default_model": "sonnet",
    "default_effort": "medium",
    "rationale": "Mechanical, deterministic, scripted transforms, scaffolding — bounded output, predictable steps."
  },
  "purely-mechanical": {
    "default_model": "haiku",
    "default_effort": "low",
    "rationale": "Purely mechanical work within the mechanical work-shape — cheapest tier still safe for bounded, predictable steps."
  },
  "read-only-survey": {
    "default_model": "sonnet",
    "default_effort": "low",
    "rationale": "Read-only survey, search, grep, sampling, census — low-effort read, no write risk."
  },
  "offload": {
    "default_model": "sonnet",
    "default_effort": "medium",
    "rationale": "External-engine delegation, intent=offload — chaperone is mechanical verify-apply-test; a heavier chaperone erases the token savings that motivated the delegation (KTD2)."
  },
  "second-opinion": {
    "default_model": "opus",
    "default_effort": "high",
    "rationale": "External-engine delegation, intent=second-opinion — adversarial verification IS the product; extra spend assumed; fable/xhigh available as a per-unit override, never a default (KTD2)."
  }
}
