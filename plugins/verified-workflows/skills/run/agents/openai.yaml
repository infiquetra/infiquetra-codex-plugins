interface:
  display_name: "Run Verified Workflow"
  short_description: "Execute a receipt-backed workflow DAG"
  default_prompt: "Use $run to execute this approved workflow with role and validator evidence."
