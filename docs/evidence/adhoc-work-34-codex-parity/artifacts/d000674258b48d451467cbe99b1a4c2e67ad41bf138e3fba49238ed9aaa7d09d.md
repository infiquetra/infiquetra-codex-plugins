---
verdict: ship-with-deferred
health_score: 98
---

# QA report — cross-runtime Outcome parity port (#34, U1–U5 pre-integrate gate)

- **Target**: branch `work/34-codex-parity` at **HEAD `480192ca8b90d028fa9b2219013f53a522a5b899`**
  (code SHA `39a9ed4` = code-review remediation; REVIEWED_SHA `aef1dea` + review-artifact commit).
  Pre-merge; destination merge via the atomic PR after the integrate-stage proofs.
- **Date**: 2026-07-20
- **Tier**: Standard (critical + high + medium block). Criteria frozen pre-check
  (`docs/evidence/adhoc-work-34-codex-parity/criteria-qa-480192ca….json`, hash `ff905b78`).
- **Scope**: behavior, security, api, config, docs (diff-aware map over 38 files from base
  `3723a818` = current merge-base with `origin/main`). No UI surface — the browser MCP check is a
  graceful no-op. The integrate-stage proofs (isolated install, fresh-session readback, rollback,
  cutover + `release_evidence` binding) are intentionally unbuilt: they are the next unit's own
  acceptance evidence, out of this gate's scope by plan sequencing.
- **Saga**: none in this repo's store (scan: 0 candidates — tick skipped, never minted; leaf tracked
  as `leaf-lease-safe-runtime-continuity-codex-parity` in the outcome ledger of
  infiquetra-claude-plugins). Baseline: none (first QA run for this thread) — delta null.

## Ship Verdict: ship-with-deferred

No critical/high/medium findings; the only open items are five dispositioned lows (P3 cross-walk)
carried as deferred-with-repro — none block at Standard.

## Health Score: 98 (baseline n/a, delta n/a)

| Risk class (in scope) | Score | Result |
|---|---|---|
| behavior | 97 | PASS (1 low, non-blocking) |
| security | 97 | PASS (1 low, non-blocking) |
| api | 100 | PASS |
| config | 100 | PASS |
| docs | 97 | PASS (1 low, non-blocking) |
| **Overall** | **98** | **ship-with-deferred** |

The score is a deterministic gstack-formula port over LLM-assigned severities — a signal, not the
gate; the verdict above is the decision.

## Checks run (fresh at HEAD, criteria frozen first)

- **behavior**: outcome suites + dispatch-migration + operator-choice + per-port gate — **168
  passed** (`PYTHONPATH=. uv run pytest tests/test_outcome_cross_runtime.py
  tests/test_outcome_command.py tests/test_outcome_dispatch_migration.py
  tests/test_outcome_cross_runtime_parity_port_contract.py
  plugins/saga/tests/test_codex_operator_choice.py -q -p no:cacheprovider`). Full battery at the
  identical code state: **2572 passed** at `39a9ed4` (HEAD delta is the 80-line review doc only —
  `git diff --stat 39a9ed4 HEAD`).
- **security**: 59-test adversarial subset (tamper/seal/credential/symlink/oversize/duplicate/
  local-path/host/receiver/halt selections) — **59 passed**; code-review security lens read all
  1597 lines of `outcome_compat.py` and confirmed fixed-argv git, `_safe_name`/32-hex path gates,
  redacted receipts, O_EXCL 0o600 sealed writes, seal re-verification on acceptance.
- **api**: per-port gate **21/21** — fixture byte-parity digests, zero-drift codex inventory pin,
  U2/U4/U5 evidence digests, capability-snapshot honesty mutations, version lineage
  (`0.103.0 → 0.77.0+codex.20260720023112`).
- **config**: `validate_codex_plugins` **green** at HEAD; KTD6 dormancy pin
  (`test_dispatcher_lease_seam_stays_dormant_ktd6`) + release-version coherence test — **2
  passed**; release surfaces coherent across plugin.json / CHANGELOG / validator pin /
  operator-choice test / target inventory.
- **docs**: legacy-token inventory digest coherence enforced by the validator (rebuilt at
  `39a9ed4`, historical pin `42c16a25…` unchanged); CHANGELOG claims verified against code by the
  code-review maintainability lens; PORTABILITY/matrix/SKILL updated with no stale bundle-flow
  mention outside the two upstream-routed items below.

## Top findings

None new at this gate. All five deferred items below were found and dispositioned by the
code-review gate (artifact `docs/code-reviews/2026-07-19-outcome-cross-runtime-parity-code-review.md`).

## Summary by severity

| Severity | Count | Blocks at Standard? |
|---|---|---|
| critical | 0 | yes |
| high | 0 | yes |
| medium | 0 | yes |
| low | 5 | no |

## Deferred (with repro)

1. **low / docs** — stale `export`/`import` `--help` strings describe the retired bundle flow
   (`plugins/saga/scripts/outcome.py:1934/1937`). Repro: `outcome export --help`. Byte-identical
   in frozen Claude `97d2fb15` (its lines 2281/2284) — **routed upstream-first** (fix in
   infiquetra-claude-plugins, re-port), per port-fidelity contract.
2. **low / behavior** — unreachable success-print after unconditional `import_bundle` refusal
   (`outcome.py:2209`). Repro: read `outcome.py:2206-2209` vs the unconditional raise at `:1657`.
   Byte-identical upstream (Claude `:2624-2627`) — **routed upstream-first**.
3. **low / security** — handoff store directory created at default umask, not 0o700
   (`outcome_compat.py:1135`); sealed records themselves are 0o600. Matches the existing
   outcome-store convention; **owned by cross-runtime-acceptance** (audit/store ancestor
   hardening, KTD6 deferral).
4. **low / behavior (advisory)** — `except _cli_broker_error()` may raise `RuntimeError` when
   fleet-core is unresolvable AND a non-halt exception fires (`outcome.py:2160`); byte-identical
   to shipped Claude; this repo resolves fleet-core via shim rung 2.
5. **low / maintainability (advisory)** — vestigial one-element loop `for flag_parser in
   (p_handoff,):` (`outcome.py:1892`); harmless graft shape.

## Recommended regression tests

The code-review remediation `39a9ed4` already added the highest-value ones (production
`_settled_lookup` both directions, CLI `attach --attend` end-to-end, admission-guard negative,
typed export-alias oracle). Remaining recommendation for the integrate unit: one installed-cache
smoke that runs `outcome discover` from the isolated profile install and validates the envelope
schema — precondition: isolated install; action: CLI discover; assertion:
`parse_discovery_envelope` accepts stdout (this belongs to the integrate stage's own evidence).

## Verdict derivation

Standard tier blocks critical+high+medium. Zero findings at those severities across all five
in-scope classes → every class passes → **ship-with-deferred** (five recorded lows carried with
repro). Gate scope is U1–U5 + review readiness for the integrate/release unit, per the frozen
criteria.
