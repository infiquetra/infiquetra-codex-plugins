interface:
  display_name: "Run Verified Workflow"
  short_description: "Resolve roles for a root-owned workflow DAG"
  default_prompt: "Use $run to resolve this approved workflow's roles and evidence contract, and report the actual runtime vehicle truthfully."
