"""Generated mission-control shim DATA. DO NOT EDIT.

Generated by tools/docs/gen_issue_contract.py from the issue_fields block in config/sdlc-schema.json (U2: full field set, faithfully extracted from card_validator.py). DATA only -- the card_validator algorithm (home-lab) is never generated (KTD2).

Whole shim data surface, shaped to drop into sdlc_manager.py's
validate_card_body (U4 swap): the required/optional H3 headers, the
header parser + the named semantic regexes (CHECKLIST/CODE_BLOCK/
PATH_LINE), the named ACCEPTANCE_EXECUTABLE regex (R2/KTD8), the per-field
SEMANTIC_CHECKS + EXECUTABLE_CHECKS, and the LOWERCASED placeholder set the
shim compares against. DATA only -- the shim algorithm is never generated
(KTD2). The shim is a pre-flight on the body alone (no Risk/type arg), so
it enforces the always-required headers + the executable-acceptance check;
the risk-conditional matrix is enforced by the authoritative home-lab gate.
"""
from __future__ import annotations

# Required / optional ### H3 headers the pre-flight shim checks for.
REQUIRED_H3_HEADERS = (
    'Objective',
    'Intent',
    'Out-of-scope / non-goals',
    'Files expected to change',
    'Tests to add or update',
    'Context library links',
    'Acceptance criteria',
    'Verification',
)

OPTIONAL_H3_HEADERS = (
    'Inputs inventory',
    'Failure modes / pre-mortem',
    'Stop conditions',
    'Notes / conventions',
    'Lifecycle Origin',
)

# The validator-wide header parser + the named semantic-check regexes the
# shim's validate_card_body references (regex source strings; the shim
# re-compiles them). Names match the shim's current hand-maintained
# _HEADER_RE / _CHECKLIST_RE / _CODE_BLOCK_RE / _PATH_LINE_RE.
HEADER_RE_PATTERN = '^###\\s+(?P<label>.+?)\\s*$'
CHECKLIST_RE_PATTERN = '^\\s*[-*]\\s+\\[[ xX]\\]\\s+\\S'
CODE_BLOCK_RE_PATTERN = '^```'
PATH_LINE_RE_PATTERN = '^\\s*(?:[-*]\\s+)?[`\'\\"]?[\\w./~\\-]*[/.][\\w./~\\-]+.*$'
# Executable-acceptance regex (R2/KTD8): the acceptance field must name a
# runnable check (a `code span` or a ``` fenced block).
ACCEPTANCE_EXECUTABLE_RE_PATTERN = '`[^`]+`|^```'

# field_key -> {pattern, flags, message} semantic checks (full DATA).
SEMANTIC_CHECKS = {
    'files_expected': {
        'pattern': '^\\s*(?:[-*]\\s+)?[`\'\\"]?[\\w./~\\-]*[/.][\\w./~\\-]+.*$',
        'flags': ('MULTILINE', ),
        'message': 'no plausible file paths found (expected lines containing `/` or a file extension)',
    },
    'acceptance_criteria': {
        'pattern': '^\\s*[-*]\\s+\\[[ xX]\\]\\s+\\S',
        'flags': ('MULTILINE', ),
        'message': 'no checklist items found (expected at least one `- [ ] ...` line)',
    },
    'verification': {
        'pattern': '^```',
        'flags': ('MULTILINE', ),
        'message': 'no fenced code block found (expected commands inside a ``` ``` block)',
    },
}

# field_key -> {pattern, flags, message} ADDITIVE executable checks.
EXECUTABLE_CHECKS = {
    'acceptance_criteria': {
        'pattern': '`[^`]+`|^```',
        'flags': ('MULTILINE', ),
        'message': 'acceptance criteria are not executable (expected each criterion to name a runnable check -- a command in a `code span` or a ``` fenced block -- with its expected result)',
    },
}

# Empty-ish placeholder seeds, LOWERCASED to match the shim's
# ``ln.lower() in _PLACEHOLDER_LINES`` comparison.
PLACEHOLDER_LINES = (
    '- [ ]',
    '-',
    '* [ ]',
    '*',
    '_no response_',
    'none',
    '<!-- placeholder -->',
)

__all__ = [
    "REQUIRED_H3_HEADERS",
    "OPTIONAL_H3_HEADERS",
    "HEADER_RE_PATTERN",
    "CHECKLIST_RE_PATTERN",
    "CODE_BLOCK_RE_PATTERN",
    "PATH_LINE_RE_PATTERN",
    "ACCEPTANCE_EXECUTABLE_RE_PATTERN",
    "SEMANTIC_CHECKS",
    "EXECUTABLE_CHECKS",
    "PLACEHOLDER_LINES",
]
