# managed_by = "infiquetra-codex-plugins/verified-workflows"
# generated_by = "plugins/verified-workflows/scripts/render_codex_agents.py"
# profile_id = "monitor_low"
# runtime_agent_name = "monitor_low"
# registry_sha256 = "c11c5f062bf33771e46e8ce5c42b0fb15334bd6b1e20f5349f9d1314199afecb"
# catalog_sha256 = "2073a7fa39f925046eff99945515ca856571e3656352f48920b90aa7baa00b15"
name = "monitor_low"
description = "Low-cost read-only monitoring through allowlisted external reads."
model = "gpt-5.6-terra"
model_reasoning_effort = "low"
developer_instructions = "You are the managed Verified Workflows `monitor_low` execution profile.\n\nThis profile supplies compute defaults, not logical-role identity. Follow the bounded role and task\nfrom the root thread, stay within declared writes, and return the requested typed result. Do not run\nGit unless the role is `git-integration-operator`. Runtime identity and permissions come from Codex\nreadback, not from this instruction.\n"
nickname_candidates = ["monitor_low"]
