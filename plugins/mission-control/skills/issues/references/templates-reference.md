# SDLC Issue Templates Reference

This file is generated by `plugins/mission-control/scripts/sync_template_docs.py` from
the canonical templates in `$INFIQUETRA_SDLC_PATH/.github/ISSUE_TEMPLATE/`.
Do not edit this file by hand; update the canonical templates or the renderer instead.

## Hermes card taxonomy

- Actionable templates: `capability.yml`, `enhancement.yml`, `defect.yml`.
- Actionable labels: `hermes-task`, `needs-plan`, plus the type label.
- Non-actionable templates: `exploration.yml`, `context-update.yml`.
- Non-actionable labels: `hermes-not-actionable` plus template-specific context labels.

## Shared actionable required fields

- Objective
- Intent
- Out-of-scope / non-goals
- Files expected to change
- Tests to add or update
- Context library links
- Acceptance criteria
- Verification

## Shared actionable risk-conditional fields

- Inputs inventory
- Failure modes / pre-mortem
- Stop conditions

## Shared actionable generated fields

- Lifecycle Origin

## Shared actionable optional fields

- Notes / conventions
- Capability size (human planning hint) on capability cards only

## Capability

- File: `capability.yml`
- Title prefix: `[CAPABILITY] `
- Hermes actionable: yes
- Labels: `capability`, `hermes-task`, `needs-plan`
- Summary: Capability represents complete end-to-end deployable functionality. Its optional Capability size field is a human planning hint only.

### Required fields

- Objective
- Intent
- Out-of-scope / non-goals
- Files expected to change
- Tests to add or update
- Context library links
- Acceptance criteria
- Verification

### Optional fields

- Inputs inventory
- Failure modes / pre-mortem
- Stop conditions
- Notes / conventions
- Lifecycle Origin
- Capability size (human planning hint)

### Hermes validator contract

- Actionable cards must carry `hermes-task`, `needs-plan`, and the type label.
- GitHub issue forms must render required fields as exact `### <Field label>` headers.
- `Context library links` is required for Hermes readiness; use `_none_` when no context applies.
- `Acceptance criteria` must include at least one `- [ ]` checklist item and name a runnable check.
- Verification must include exact commands in a fenced shell code block.
- High and very-high risk cards also require `Inputs inventory`, `Failure modes / pre-mortem`, and `Stop conditions`.
- `Lifecycle Origin` is auto-populated by prepared handoff flows, not author supplied.

## Enhancement

- File: `enhancement.yml`
- Title prefix: `[ENHANCEMENT] `
- Hermes actionable: yes
- Labels: `enhancement`, `hermes-task`, `needs-plan`
- Summary: Enhancement improves existing functionality without creating a new capability.

### Required fields

- Objective
- Intent
- Out-of-scope / non-goals
- Files expected to change
- Tests to add or update
- Context library links
- Acceptance criteria
- Verification

### Optional fields

- Inputs inventory
- Failure modes / pre-mortem
- Stop conditions
- Notes / conventions
- Lifecycle Origin

### Hermes validator contract

- Actionable cards must carry `hermes-task`, `needs-plan`, and the type label.
- GitHub issue forms must render required fields as exact `### <Field label>` headers.
- `Context library links` is required for Hermes readiness; use `_none_` when no context applies.
- `Acceptance criteria` must include at least one `- [ ]` checklist item and name a runnable check.
- Verification must include exact commands in a fenced shell code block.
- High and very-high risk cards also require `Inputs inventory`, `Failure modes / pre-mortem`, and `Stop conditions`.
- `Lifecycle Origin` is auto-populated by prepared handoff flows, not author supplied.

## Defect

- File: `defect.yml`
- Title prefix: `[DEFECT] `
- Hermes actionable: yes
- Labels: `defect`, `hermes-task`, `needs-plan`
- Summary: Defect captures broken behavior that a Hermes agent can plan and fix.

### Required fields

- Objective
- Intent
- Out-of-scope / non-goals
- Files expected to change
- Tests to add or update
- Context library links
- Acceptance criteria
- Verification

### Optional fields

- Inputs inventory
- Failure modes / pre-mortem
- Stop conditions
- Notes / conventions
- Lifecycle Origin

### Hermes validator contract

- Actionable cards must carry `hermes-task`, `needs-plan`, and the type label.
- GitHub issue forms must render required fields as exact `### <Field label>` headers.
- `Context library links` is required for Hermes readiness; use `_none_` when no context applies.
- `Acceptance criteria` must include at least one `- [ ]` checklist item and name a runnable check.
- Verification must include exact commands in a fenced shell code block.
- High and very-high risk cards also require `Inputs inventory`, `Failure modes / pre-mortem`, and `Stop conditions`.
- `Lifecycle Origin` is auto-populated by prepared handoff flows, not author supplied.

## Exploration

- File: `exploration.yml`
- Title prefix: `[EXPLORATION] `
- Hermes actionable: no
- Labels: `exploration`, `research`, `hermes-not-actionable`
- Summary: Exploration produces research, recommendations, or POC findings rather than production code.

### Required fields

- Research Question
- Context
- Success Criteria
- Timebox
- Deliverables
- Key Questions to Answer

### Optional fields

- Evaluation Criteria
- Investigation Approach
- Parent Objective (optional)
- Parent Initiative (optional)
- Automated Tests (for POC validation)
- Manual/E2E Tests (for POC evaluation)
- AI Generation Instructions
- Related Issues

### Hermes behavior

- Non-actionable cards must carry `hermes-not-actionable`.
- Do not document or treat this template as a Hermes actionable task card.
- Use these cards for coordination, research, or documentation context rather than agent dispatch.

## Context Update

- File: `context-update.yml`
- Title prefix: `[CONTEXT] `
- Hermes actionable: no
- Labels: `context-update`, `documentation`, `hermes-not-actionable`
- Summary: Context Update maintains Blueprint documentation and is not dispatched as a Hermes task.

### Required fields

- What's Being Updated?
- Why This Update?
- Files to Update
- Changes Description
- Acceptance Criteria
- Critical Context Gap?
- Documentation Type

### Optional fields

- AI Prompt to Save (if applicable)
- Related Issues
- Parent Objective (optional)
- Parent Initiative (optional)
- Documentation Quality Checklist
- Automated Tests (for documentation validation)
- Manual/E2E Tests (for documentation review)
- AI Generation Instructions

### Hermes behavior

- Non-actionable cards must carry `hermes-not-actionable`.
- Do not document or treat this template as a Hermes actionable task card.
- Use these cards for coordination, research, or documentation context rather than agent dispatch.
