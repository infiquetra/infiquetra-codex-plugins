# Codex Spawn Ownership

The root Codex thread owns native subagent spawn, follow-up, waiting, and integration. Saga does not
claim a Goal, hook, fork, or generic child as a leaf backend. Goal may bind continuation only after
the Goal tool returns a stable identifier. Requested model and sandbox labels are not enforcement
proof without the corresponding runtime receipt.
