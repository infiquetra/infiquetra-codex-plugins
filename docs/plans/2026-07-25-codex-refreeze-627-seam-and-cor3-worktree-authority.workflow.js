// ===========================================================================
// codex-refreeze-627-seam-and-cor3-worktree-authority -- emitted Claude Code workflow harness.
// AUTO-EMITTED from a structured execution-spec by execution_spec.py.
// CONTROL FLOW ONLY -- every agent reads the plan as its authoritative spec.
// Per-unit {model, effort} tiers (R2(b)); R3 pilot/fan-out same-tier +
// R10 enumerated-target reconciliation enforced at emit time.
// ===========================================================================

export const meta = {
  name: "codex-refreeze-627-seam-and-cor3-worktree-authority",
  description: "Re-freeze the codex cross-runtime seam at Claude b464d090, then port refuse-mode lease admission, the dispatcher transient-error arm, and the COR3 worktree lease-authority subsystem into codex-native modules.",
}
const settlement = {"casualty_threshold_percent":0,"dispatch_id":"workflow:8639e6bb6562cee22a673994","driver":{"invocation_id":null,"units":[{"return_keys":[{"deliverable":"return:new_contract_path","result_key":"new_contract_path"},{"deliverable":"return:source_rows_classified","result_key":"source_rows_classified"},{"deliverable":"return:expected_count","result_key":"expected_count"},{"deliverable":"return:inventory_sha256","result_key":"inventory_sha256"},{"deliverable":"return:promoted_defer_rows","result_key":"promoted_defer_rows"},{"deliverable":"return:unit_id_mapping","result_key":"unit_id_mapping"},{"deliverable":"return:classification_gate_result","result_key":"classification_gate_result"},{"deliverable":"return:verify_source_result","result_key":"verify_source_result"},{"deliverable":"return:test_results","result_key":"test_results"},{"deliverable":"return:structural_blockers","result_key":"structural_blockers"},{"deliverable":"return:stop_reason","result_key":"stop_reason"}],"settlement_unit_id":"U1","workflow_unit_id":"U1"},{"return_keys":[{"deliverable":"return:changed_files","result_key":"changed_files"},{"deliverable":"return:byte_identity_diff","result_key":"byte_identity_diff"},{"deliverable":"return:new_test_files","result_key":"new_test_files"},{"deliverable":"return:red_first_evidence","result_key":"red_first_evidence"},{"deliverable":"return:must_pass_both_evidence","result_key":"must_pass_both_evidence"},{"deliverable":"return:structural_blockers","result_key":"structural_blockers"},{"deliverable":"return:stop_reason","result_key":"stop_reason"}],"settlement_unit_id":"U2","workflow_unit_id":"U2"},{"return_keys":[{"deliverable":"return:ktd6_determination","result_key":"ktd6_determination"},{"deliverable":"return:changed_files","result_key":"changed_files"},{"deliverable":"return:public_api_ported","result_key":"public_api_ported"},{"deliverable":"return:refuse_branch_impl","result_key":"refuse_branch_impl"},{"deliverable":"return:test_results","result_key":"test_results"},{"deliverable":"return:red_first_evidence","result_key":"red_first_evidence"},{"deliverable":"return:upstream_defect_found","result_key":"upstream_defect_found"},{"deliverable":"return:stop_reason","result_key":"stop_reason"}],"settlement_unit_id":"U3","workflow_unit_id":"U3"},{"return_keys":[{"deliverable":"return:changed_files","result_key":"changed_files"},{"deliverable":"return:transient_class_impl","result_key":"transient_class_impl"},{"deliverable":"return:classifier_impl","result_key":"classifier_impl"},{"deliverable":"return:reraise_ordering_impl","result_key":"reraise_ordering_impl"},{"deliverable":"return:test_results","result_key":"test_results"},{"deliverable":"return:red_first_evidence","result_key":"red_first_evidence"},{"deliverable":"return:board_sync_unmodified_pass","result_key":"board_sync_unmodified_pass"},{"deliverable":"return:stop_reason","result_key":"stop_reason"}],"settlement_unit_id":"U4","workflow_unit_id":"U4"},{"return_keys":[{"deliverable":"return:ported_symbols","result_key":"ported_symbols"},{"deliverable":"return:codex_adaptations","result_key":"codex_adaptations"},{"deliverable":"return:changed_files","result_key":"changed_files"},{"deliverable":"return:test_results","result_key":"test_results"},{"deliverable":"return:red_first_evidence","result_key":"red_first_evidence"},{"deliverable":"return:compatibility_floor_result","result_key":"compatibility_floor_result"},{"deliverable":"return:stop_reason","result_key":"stop_reason"}],"settlement_unit_id":"U5","workflow_unit_id":"U5"},{"return_keys":[{"deliverable":"return:release_surface_diffs","result_key":"release_surface_diffs"},{"deliverable":"return:versions_chosen","result_key":"versions_chosen"},{"deliverable":"return:journal_entries","result_key":"journal_entries"},{"deliverable":"return:acceptance_leg_matrix","result_key":"acceptance_leg_matrix"},{"deliverable":"return:legs_changed","result_key":"legs_changed"},{"deliverable":"return:legs_still_red","result_key":"legs_still_red"},{"deliverable":"return:inventory_rebuilt_sha256","result_key":"inventory_rebuilt_sha256"},{"deliverable":"return:unit_gate_results","result_key":"unit_gate_results"},{"deliverable":"return:cutover_gate_result","result_key":"cutover_gate_result"},{"deliverable":"return:stop_reason","result_key":"stop_reason"}],"settlement_unit_id":"U6","workflow_unit_id":"U6"}]},"max_attempts":3,"schema":"dispatch_settlement.v1","site":"workflow","units":[{"deliverables":["structured-result","return:new_contract_path","return:source_rows_classified","return:expected_count","return:inventory_sha256","return:promoted_defer_rows","return:unit_id_mapping","return:classification_gate_result","return:verify_source_result","return:test_results","return:structural_blockers","return:stop_reason"],"idempotency_key":"workflow:8639e6bb6562cee22a673994:U1","unit_id":"U1"},{"deliverables":["structured-result","return:changed_files","return:byte_identity_diff","return:new_test_files","return:red_first_evidence","return:must_pass_both_evidence","return:structural_blockers","return:stop_reason"],"idempotency_key":"workflow:8639e6bb6562cee22a673994:U2","unit_id":"U2"},{"deliverables":["structured-result","return:ktd6_determination","return:changed_files","return:public_api_ported","return:refuse_branch_impl","return:test_results","return:red_first_evidence","return:upstream_defect_found","return:stop_reason"],"idempotency_key":"workflow:8639e6bb6562cee22a673994:U3","unit_id":"U3"},{"deliverables":["structured-result","return:changed_files","return:transient_class_impl","return:classifier_impl","return:reraise_ordering_impl","return:test_results","return:red_first_evidence","return:board_sync_unmodified_pass","return:stop_reason"],"idempotency_key":"workflow:8639e6bb6562cee22a673994:U4","unit_id":"U4"},{"deliverables":["structured-result","return:ported_symbols","return:codex_adaptations","return:changed_files","return:test_results","return:red_first_evidence","return:compatibility_floor_result","return:stop_reason"],"idempotency_key":"workflow:8639e6bb6562cee22a673994:U5","unit_id":"U5"},{"deliverables":["structured-result","return:release_surface_diffs","return:versions_chosen","return:journal_entries","return:acceptance_leg_matrix","return:legs_changed","return:legs_still_red","return:inventory_rebuilt_sha256","return:unit_gate_results","return:cutover_gate_result","return:stop_reason"],"idempotency_key":"workflow:8639e6bb6562cee22a673994:U6","unit_id":"U6"}]}
const lease = {"aggregate_limit":7,"batch_id":null,"claim_ttl_seconds":30,"execution_ttl_seconds":300,"generated_runtime_filesystem_access":false,"invocation_id":null,"mutation":"read-write","owner_id":null,"policy_sha256":"b985631b1a874a0817a3cac49b27565875fb5dedd3ad2008cf397dc640d43a0a","requires_prelaunch_reservation":true,"reservation_width":1,"schema":"workflow_lease_reservation.v1","session_limit":1,"slots":["slot-001"],"spec_sha256":"8639e6bb6562cee22a6739945cf2af23bd0feb6548c35d20cf92950c6fcc075d","workload_unit_ids":["U1","U2","U3","U4","U5","U6"]}

const REPO = "infiquetra/infiquetra-codex-plugins"

const __pulledCords = []

function __gate(result, opts) {
  const unitId = opts.unitId || "unknown";

  function isEmptyOrAbsent(val) {
    if (val === null || val === undefined) return true;
    if (typeof val === 'string') return val.trim() === '';
    if (Array.isArray(val)) return val.length === 0;
    if (val instanceof Map || val instanceof Set) return val.size === 0;
    if (typeof val === 'object') return Object.keys(val).length === 0;
    return false;
  }

  function parseResult(val) {
    if (typeof val === 'string') {
      let s = val.trim();
      if (s.startsWith('```')) {
        const lines = s.split('\n');
        if (lines.length >= 2) {
          if (lines[0].startsWith('```')) {
            lines.shift();
          }
          if (lines.length && lines[lines.length - 1].trim() === '```') {
            lines.pop();
          }
          s = lines.join('\n').trim();
        }
      }
      if (s.startsWith('{') || s.startsWith('[')) {
        try {
          return JSON.parse(s);
        } catch (e) {
          // fall through to embedded-JSON extraction
        }
      }
      // Extract an embedded JSON value when the agent prepends conversational prose
      // before the object (sonnet/opus routinely add a "looks good, tests pass" preamble
      // ahead of the return object). Try object first, then array.
      const pairs = [['{', '}'], ['[', ']']];
      for (let i = 0; i < pairs.length; i++) {
        const start = s.indexOf(pairs[i][0]);
        const end = s.lastIndexOf(pairs[i][1]);
        if (start !== -1 && end > start) {
          try {
            return JSON.parse(s.slice(start, end + 1));
          } catch (e) {
            // try the next delimiter pair
          }
        }
      }
    }
    return val;
  }

  // #364 R7: pull_cord -- the worker-initiated out-of-depth disposition, a valid alternative
  // to the return contract (distinct from success and from the missing/malformed throws).
  // Cords batch into __pulledCords for ONE coordinator escalation entry (R8); the unit is
  // never marked complete because the batched check fails the run before it returns.
  const cordProbe = parseResult(result);
  if (cordProbe && typeof cordProbe === 'object' && !Array.isArray(cordProbe)
      && typeof cordProbe.pull_cord === 'string' && cordProbe.pull_cord.trim() !== '') {
    __pulledCords.push({ unit: unitId, reason: cordProbe.pull_cord.trim(),
                         proposal: opts.cordProposal || null });
    return result;
  }

  if (opts.expectsOutput && isEmptyOrAbsent(result)) {
    throw new Error(
      `missing-output: Unit ${unitId} expected structured output but received none or empty.`
    );
  }

  if (typeof result === 'string') {
    let s = result.trim();
    if (s.startsWith('```')) {
      const lines = s.split('\n');
      if (lines.length >= 2) {
        if (lines[0].startsWith('```')) {
          lines.shift();
        }
        if (lines.length && lines[lines.length - 1].trim() === '```') {
          lines.pop();
        }
        s = lines.join('\n').trim();
      }
    }
    if (s.startsWith('{') || s.startsWith('[')) {
      try {
        JSON.parse(s);
      } catch (e) {
        throw new Error(
          `malformed-output: Unit ${unitId} output is a structurally truncated JSON: ${e.message}`
        );
      }
    }
  }

  let targetCount = null;
  if (opts.targets !== undefined && opts.targets !== null) {
    if (typeof opts.targets === 'number') {
      targetCount = opts.targets;
    } else if (Array.isArray(opts.targets)) {
      targetCount = opts.targets.length;
    }
  }

  if (targetCount !== null) {
    const parsed = parseResult(result);
    let producedCount = 0;
    if (parsed !== null && parsed !== undefined) {
      if (Array.isArray(parsed)) {
        producedCount = parsed.length;
      } else if (parsed instanceof Map || parsed instanceof Set) {
        producedCount = parsed.size;
      } else if (typeof parsed === 'object') {
        producedCount = Object.keys(parsed).length;
      } else {
        producedCount = isEmptyOrAbsent(parsed) ? 0 : 1;
      }
    }
    if (producedCount < targetCount) {
      const shortfall = targetCount - producedCount;
      throw new Error(
        `missing-output: Unit ${unitId} produced fewer items than expected. ` +
        `Expected ${targetCount}, produced ${producedCount}. Shortfall: ${shortfall}.`
      );
    }
  }

  if (opts.returns && opts.returns.length > 0) {
    const parsed = parseResult(result);
    if (parsed === null || parsed === undefined || typeof parsed !== 'object' || Array.isArray(parsed)) {
      throw new Error(
        `missing-output: Unit ${unitId} result is not a structured dictionary. ` +
        `Missing required keys: ${opts.returns.join(', ')}.`
      );
    }
    const missing = opts.returns.filter(
      k => !(k in parsed) || parsed[k] === null || parsed[k] === undefined
    );
    if (missing.length > 0) {
      throw new Error(
        `missing-output: Unit ${unitId} output is missing required keys: ${missing.join(', ')}.`
      );
    }
  }

  return result;
}

function __is429(x) {
  if (x === null || x === undefined) return false;
  if (typeof x === 'number') return x === 429;
  if (typeof x === 'string') return /(^|[^0-9])429([^0-9]|$)/.test(x) || /rate[\s_-]?limit/i.test(x);
  var status = x.status || x.statusCode || x.status_code || x.code;
  if (status === 429 || status === '429') return true;
  if (x.rateLimited === true || x.rate_limited === true) return true;
  var msg = x.message || x.error || '';
  return typeof msg === 'string' && (/(^|[^0-9])429([^0-9]|$)/.test(msg) || /rate[\s_-]?limit/i.test(msg));
}

function __retryAfterMs(signal) {
  if (signal === null || typeof signal !== 'object') return null;
  if (typeof signal.retryAfterMs === 'number') return signal.retryAfterMs;
  if (typeof signal.retryAfter === 'number') return signal.retryAfter * 1000;
  if (typeof signal.retry_after === 'number') return signal.retry_after * 1000;
  return null;
}

function __retryBackoffMs(attempt, baseMs, maxMs, retryAfterMs) {
  if (typeof retryAfterMs === 'number' && retryAfterMs > 0) {
    return Math.min(retryAfterMs, maxMs);
  }
  return Math.min(baseMs * Math.pow(2, attempt - 1), maxMs);
}

async function __retry(thunk, opts) {
  var o = opts || {};
  var maxAttempts = o.maxAttempts || 3;
  var baseMs = o.baseMs || 1000;
  var maxMs = o.maxMs || 60000;
  var sleep = o.sleep || function (ms) {
    return new Promise(function (r) {
      if (typeof setTimeout === 'function') { setTimeout(r, ms); } else { r(); }
    });
  };
  var attempt = 0;
  while (true) {
    attempt++;
    var result;
    var threw = false;
    var caught = null;
    try {
      result = await thunk();
    } catch (err) {
      threw = true;
      caught = err;
    }
    var signal = threw ? caught : result;
    if (__is429(signal) && attempt < maxAttempts) {
      await sleep(__retryBackoffMs(attempt, baseMs, maxMs, __retryAfterMs(signal)));
      continue;
    }
    if (threw) throw caught;
    return result;
  }
}

function __verifierPrompt(basePrompt, unitResult) {
  var rendered;
  try {
    rendered = JSON.stringify(unitResult, null, 2);
  } catch (err) {
    rendered = String(unitResult);
  }
  var repoLine = (typeof REPO === "string")
    ? `PRIMARY REPO PATH: ${REPO}`
    : "PRIMARY REPO PATH: not declared by this workflow";
  return `${basePrompt}

VERIFIER VISIBILITY PROTOCOL (#519):
${repoLine}
- You run in a disposable verifier worktree. Before judging file content, capture the primary
  checkout SHA with: git -C <primary repo path> rev-parse HEAD
- Materialize that exact SHA in your verifier worktree with: git checkout <sha> -- .
- If the unit result names uncommitted files or diffs, inspect the primary checkout read-only
  with git -C <primary repo path> status --short and git -C <primary repo path> diff / diff --
  <path>. For named untracked output files, read the primary checkout path directly; never mutate
  the primary checkout.
- Return examined_sha as the SHA you actually materialized or inspected. If you cannot see enough
  evidence to judge, return a refuted entry explaining the visibility gap; do not emit prose-only
  "nothing to verify" output.

UNIT RESULT INPUT (authoritative structured evidence):
${rendered}`;
}

// ---- U1: port-contract bootstrap and mandatory classification gate ----
const U1 = await agent(
  "WORKING DIRECTORY -- READ THIS FIRST, IT IS LOAD-BEARING.\n\nEvery relative path in this prompt belongs to the CODEX repo:\n  /Users/jefcox/workspace/infiquetra/infiquetra-codex-plugins\nThat is NOT this session's default working directory. The default cwd is\n  /Users/jefcox/workspace/infiquetra/infiquetra-claude-plugins\nwhich is the UPSTREAM CLAUDE repo. It is READ-ONLY REFERENCE for this work and it\ncarries the operator's uncommitted files. Never edit, stage, or commit anything there.\n\nWHY THIS MATTERS: several target files exist at the SAME relative path in BOTH repos --\nplugins/saga/scripts/outcome_compat.py, plugins/saga/scripts/outcome.py,\nplugins/saga/scripts/outcome_dispatcher.py, plugins/saga/scripts/outcome_worktrees.py,\nand plugins/fleet-core/scripts/fleet_commons/lease_broker.py among them. Editing the\nwrong copy is a SILENT catastrophic failure: the edit applies cleanly, the suite passes,\nand nothing you were asked to change actually changed.\n\nMANDATORY GUARDS:\n  - The shell cwd RESETS between Bash calls in this environment. Begin every Bash\n    command with `cd /Users/jefcox/workspace/infiquetra/infiquetra-codex-plugins && ...` or use fully absolute paths.\n  - Use ABSOLUTE paths for every Read / Edit / Write call.\n  - Before your first edit, run:\n      git -C /Users/jefcox/workspace/infiquetra/infiquetra-codex-plugins rev-parse --show-toplevel\n    and confirm it prints the codex path. If it does not, STOP and report a\n    structural blocker instead of proceeding.\n  - When you need the upstream reference implementation, read it from /Users/jefcox/workspace/infiquetra/infiquetra-claude-plugins\n    at origin/main b464d090 -- read only, never write.\n\nSTAGING DISCIPLINE: this repo does NOT git-ignore `.claude/`, so the saga tick sits in\na committable path. NEVER run `git add -A` or `git add .` here. Stage explicit file\npaths only. Never stage `.claude/` or anything under it.\n\nBRANCH: `work/45-codex-refreeze-627-seam-cor3` is already checked out in the codex repo. Do not create or switch\nbranches. Commit your own unit's files with a conventional message ending in the\ntrailer `Claude-Session: https://claude.ai/code/session_014s77CGxva8ryGNQFCuYT77` and NO attribution footer of any kind. Do NOT push, do NOT open a\nPR, do NOT merge -- the driving session owns every outward mutation.\n\nRTK NOTE: this environment BLANKS `git diff | grep` pipelines. Write diffs to a file\nand parse them with python.\n\n--- unit prompt follows ---\n\nU1: Bootstrap the port contract and pass the mandatory classification gate. Read docs/plans/2026-07-25-codex-refreeze-627-seam-and-cor3-worktree-authority-plan.md as your authoritative spec; U1, requirements R4/R4a/R4b, and KTD2/KTD8 govern this unit.\n\nTHIS UNIT WRITES NO PRODUCTION CODE. Its deliverable is a classified contract that passes `validate --stage classification`. Every later unit claims rows from it. Per the runbook (docs/portability/claude-to-codex-plugin-port-runbook.md, steps 3-5), classification precedes source behavior work -- \"the classification gate proves complete treatment before source behavior work.\"\n\nUSE THE REPO'S OWN TOOLING: scripts/port_contract.py (init | refresh | validate | verify-source | render). Do NOT hand-author the manifest JSON.\n\n(a) NEW CONTRACT for the five surfaces that changed in cf15a09f..b464d090:\n\n    python3 scripts/port_contract.py init \\\n      --source-repo /Users/jefcox/workspace/infiquetra/infiquetra-claude-plugins \\\n      --source-base cf15a09f --source-target b464d090 \\\n      --source-pathspec plugins/saga/scripts/outcome_compat.py \\\n      --source-pathspec plugins/fleet-core/scripts/fleet_commons/audit_store.py \\\n      --source-pathspec plugins/fleet-core/scripts/fleet_commons/lease_broker.py \\\n      --source-pathspec plugins/saga/scripts/outcome.py \\\n      --source-pathspec plugins/saga/scripts/outcome_dispatcher.py\n\nTHE RANGE IS NOT ARBITRARY: cf15a09f is the target_ref of the predecessor contract docs/portability/ports/2026-07-19-lease-safe-substrate.json, so the two contracts chain exactly -- no gap, no re-freeze of an already-frozen range (the runbook forbids extending one). Pinning base==target (b464d090..b464d090) yields ZERO rows; verified.\n\nexpected_count IS DERIVED, NEVER ASSERTED. port_contract.py:437-450 sets expected_count = len(source_rows) from the base->target diff, NOT len(pathspecs). Measured, that range yields five M rows. Do not write the number by hand and do not \"fix\" it to match the pathspec count.\n\nClassify every row with state, treatment, rationale, units, planned_targets, planned_tests. _validate_source_rows (port_contract.py:750-786) REJECTS a direct-port/codex-adapt row lacking planned targets and tests, and REJECTS a defer/reject row that claims a unit. Map rows to this plan's units: outcome_compat.py + audit_store.py -> U2; lease_broker.py -> U3; outcome_dispatcher.py + outcome.py -> U4.\n\n(b) PROMOTE THE THREE ORPHANED DEFERS in docs/portability/ports/2026-07-19-lease-safe-substrate.json. COR3's source rows do NOT appear in the new range -- outcome_worktrees.py has ZERO changed lines across cf15a09f..b464d090, so a new contract produces nothing to classify. Its row already exists in the PREDECESSOR contract at state=classified, treatment=defer, rationale \"Worktree reconciliation consumes the guard at Claude seams; Codex worktree parity is #34 scope.\" Two siblings carry the same deferral: plugins/saga/scripts/outcome.py and tests/test_outcome_worktrees.py.\n\ncodex#34 CLOSED 2026-07-20 WITHOUT TREATING THEM, so all three are orphaned defers pointing at a discharged issue, and #45 is their correct home. Promote outcome_worktrees.py and tests/test_outcome_worktrees.py from defer to codex-adapt with planned_targets, planned_tests, and a rationale naming #45 as the discharge of the closed-#34 deferral. Re-justify outcome.py's row with a current rationale if it stays deferred there.\n\nUNIT IDS U2, U3, U4 AND U5 ARE ALREADY CLAIMED IN THAT PREDECESSOR CONTRACT. UNIT_IDS is U1-U9 (port_contract.py:84). The promoted rows must claim a FREE id -- use U6 or U7 there -- and record the mapping to this plan's U5 in the rationale. Verify which ids are free by reading the manifest; do not reuse a claimed one.\n\n`refresh` IS THE WRONG TOOL for (b): refresh_manifest (port_contract.py:1548-1578) recomputes observations and records added/removed row ids but NEVER touches state or treatment. Promotion is an operator edit that the validator then checks.\n\nGATES -- both must pass, and they are this unit's completion condition (not \"the files were written\"):\n  python3 scripts/port_contract.py validate --stage classification --manifest <each of the two manifests>\n  python3 scripts/port_contract.py verify-source --source-repo /Users/jefcox/workspace/infiquetra/infiquetra-claude-plugins --manifest <each>\nverify-source must still pass on the predecessor after the promotion, because a classification edit does not change the frozen inventory. If it fails, you changed something you should not have.\n\nTESTS: create tests/test_codex_627_seam_refreeze_port_contract.py following the shape of the five existing tests/test_*_port_contract.py modules. NOT plugins/saga/tests/test_provenance_manifest.py -- that governs a different artifact family and is the wrong home. Cover: classification gate exits 0; verify-source passes; expected_count == len(source.rows) (derived, explicitly not len(pathspecs)); inventory_sha256 recomputes; every planned_target path exists in the codex tree (this guards KTD6's two-same-named-files trap); no row left unclassified; the promoted row claims an unclaimed unit id. FAILURE MODES: init refuses to overwrite an existing manifest; a codex-adapt row missing planned_tests fails validation; a defer row carrying a units entry fails validation.\n\nSTOP AND REPORT rather than improvising if the measured row count is not five, if cf15a09f is not the predecessor's target_ref, or if a promotion would require reusing a claimed unit id.\n\nRETURN CONTRACT (all tiers): your FINAL message MUST be ONLY a single JSON object with the keys new_contract_path, source_rows_classified, expected_count, inventory_sha256, promoted_defer_rows, unit_id_mapping, classification_gate_result, verify_source_result, test_results, structural_blockers, stop_reason -- no prose, no markdown code fences, no YAML, and nothing before or after the JSON. The workflow gate parses this final message as JSON and FAILS the unit if it is not a JSON object carrying these keys. Emit it as your last action even if the work is only partial (fill each key with what you have plus a short note).",
  { label: "port-contract bootstrap and mandatory classification gate", model: "opus", effort: "high", schema: {"additionalProperties": true, "properties": {"classification_gate_result": {}, "expected_count": {}, "inventory_sha256": {}, "new_contract_path": {}, "promoted_defer_rows": {}, "source_rows_classified": {}, "stop_reason": {}, "structural_blockers": {}, "test_results": {}, "unit_id_mapping": {}, "verify_source_result": {}}, "required": ["new_contract_path", "source_rows_classified", "expected_count", "inventory_sha256", "promoted_defer_rows", "unit_id_mapping", "classification_gate_result", "verify_source_result", "test_results", "structural_blockers", "stop_reason"], "type": "object"} },
)
__gate(U1, { unitId: "U1", expectsOutput: true, returns: ["new_contract_path", "source_rows_classified", "expected_count", "inventory_sha256", "promoted_defer_rows", "unit_id_mapping", "classification_gate_result", "verify_source_result", "test_results", "structural_blockers", "stop_reason"], cordProposal: "opus/high -> opus/xhigh (+1 effort rung)" })

// ---- U2: byte re-freeze of outcome_compat.py and the audit_store ancestor guard at Claude b464d090 ----
// depends_on: U1 (barrier)
const U2 = await agent(
  "WORKING DIRECTORY -- READ THIS FIRST, IT IS LOAD-BEARING.\n\nEvery relative path in this prompt belongs to the CODEX repo:\n  /Users/jefcox/workspace/infiquetra/infiquetra-codex-plugins\nThat is NOT this session's default working directory. The default cwd is\n  /Users/jefcox/workspace/infiquetra/infiquetra-claude-plugins\nwhich is the UPSTREAM CLAUDE repo. It is READ-ONLY REFERENCE for this work and it\ncarries the operator's uncommitted files. Never edit, stage, or commit anything there.\n\nWHY THIS MATTERS: several target files exist at the SAME relative path in BOTH repos --\nplugins/saga/scripts/outcome_compat.py, plugins/saga/scripts/outcome.py,\nplugins/saga/scripts/outcome_dispatcher.py, plugins/saga/scripts/outcome_worktrees.py,\nand plugins/fleet-core/scripts/fleet_commons/lease_broker.py among them. Editing the\nwrong copy is a SILENT catastrophic failure: the edit applies cleanly, the suite passes,\nand nothing you were asked to change actually changed.\n\nMANDATORY GUARDS:\n  - The shell cwd RESETS between Bash calls in this environment. Begin every Bash\n    command with `cd /Users/jefcox/workspace/infiquetra/infiquetra-codex-plugins && ...` or use fully absolute paths.\n  - Use ABSOLUTE paths for every Read / Edit / Write call.\n  - Before your first edit, run:\n      git -C /Users/jefcox/workspace/infiquetra/infiquetra-codex-plugins rev-parse --show-toplevel\n    and confirm it prints the codex path. If it does not, STOP and report a\n    structural blocker instead of proceeding.\n  - When you need the upstream reference implementation, read it from /Users/jefcox/workspace/infiquetra/infiquetra-claude-plugins\n    at origin/main b464d090 -- read only, never write.\n\nSTAGING DISCIPLINE: this repo does NOT git-ignore `.claude/`, so the saga tick sits in\na committable path. NEVER run `git add -A` or `git add .` here. Stage explicit file\npaths only. Never stage `.claude/` or anything under it.\n\nBRANCH: `work/45-codex-refreeze-627-seam-cor3` is already checked out in the codex repo. Do not create or switch\nbranches. Commit your own unit's files with a conventional message ending in the\ntrailer `Claude-Session: https://claude.ai/code/session_014s77CGxva8ryGNQFCuYT77` and NO attribution footer of any kind. Do NOT push, do NOT open a\nPR, do NOT merge -- the driving session owns every outward mutation.\n\nRTK NOTE: this environment BLANKS `git diff | grep` pipelines. Write diffs to a file\nand parse them with python.\n\n--- unit prompt follows ---\n\nU2: Re-freeze the two byte-frozen cross-runtime twins in infiquetra-codex-plugins. Read docs/plans/2026-07-25-codex-refreeze-627-seam-and-cor3-worktree-authority-plan.md as your authoritative spec; U2 and requirements R1-R3 and KTD1/KTD3/KTD7 govern this unit. U1 has already classified this unit's rows in the port contract; claim them, do not re-derive them.\n\nSource of truth is the Claude clone at /Users/jefcox/workspace/infiquetra/infiquetra-claude-plugins, origin/main b464d090. Make plugins/saga/scripts/outcome_compat.py byte-identical to Claude's except RUNTIME_LABEL = \"codex\" (line 83), and re-port the universal fail-closed ancestor walk into plugins/fleet-core/scripts/fleet_commons/audit_store.py. Measured divergence is +40/-26 and +42/-31 respectively; the entire outcome_compat delta is RUNTIME_LABEL, the _handoff_store_halt supported=/next_action= strings, and the body plus docstring of _refuse_unsafe_handoff_ancestors().\n\nThe semantic change: drop the is_relative_to(home) early return, build the component list from candidate.anchor down, walk every component with lstat (never resolve inside the walk), refuse a symlink anywhere, and refuse world-writable UNLESS also sticky (stat.S_ISVTX). Add the NFS/SMB and FAT32/exFAT relocate guidance to next_action.\n\nCreate plugins/saga/tests/test_outcome_compat.py (it does not exist -- outcome_compat.py is 1686 lines and currently untested codex-side, so a green suite is NOT evidence per KTD7) and extend plugins/fleet-core/tests/test_audit_store.py. Cover the seven scenarios U2 lists in the plan, marking which are red-first and which are must-pass-before-and-after.\n\nThe real oracle is a byte-identity diff against Claude's file asserting RUNTIME_LABEL is the only differing line. IMPORTANT: this environment blanks `git diff | grep` pipelines -- write the diff to a file and parse it with python.\n\nRETURN CONTRACT (all tiers): your FINAL message MUST be ONLY a single JSON object with the keys changed_files, byte_identity_diff, new_test_files, red_first_evidence, must_pass_both_evidence, structural_blockers, stop_reason -- no prose, no markdown code fences, no YAML, and nothing before or after the JSON. The workflow gate parses this final message as JSON and FAILS the unit if it is not a JSON object carrying these keys. Emit it as your last action even if the work is only partial (fill each key with what you have plus a short note).",
  { label: "byte re-freeze of outcome_compat.py and the audit_store ancestor guard at Claude b464d090", model: "sonnet", effort: "high", schema: {"additionalProperties": true, "properties": {"byte_identity_diff": {}, "changed_files": {}, "must_pass_both_evidence": {}, "new_test_files": {}, "red_first_evidence": {}, "stop_reason": {}, "structural_blockers": {}}, "required": ["changed_files", "byte_identity_diff", "new_test_files", "red_first_evidence", "must_pass_both_evidence", "structural_blockers", "stop_reason"], "type": "object"} },
)
__gate(U2, { unitId: "U2", expectsOutput: true, returns: ["changed_files", "byte_identity_diff", "new_test_files", "red_first_evidence", "must_pass_both_evidence", "structural_blockers", "stop_reason"], cordProposal: "sonnet/high -> sonnet/xhigh (+1 effort rung)" })

// ---- U3: refuse-mode lease admission in the codex fleet-core broker ----
// depends_on: U2 (barrier)
const U3 = await agent(
  "WORKING DIRECTORY -- READ THIS FIRST, IT IS LOAD-BEARING.\n\nEvery relative path in this prompt belongs to the CODEX repo:\n  /Users/jefcox/workspace/infiquetra/infiquetra-codex-plugins\nThat is NOT this session's default working directory. The default cwd is\n  /Users/jefcox/workspace/infiquetra/infiquetra-claude-plugins\nwhich is the UPSTREAM CLAUDE repo. It is READ-ONLY REFERENCE for this work and it\ncarries the operator's uncommitted files. Never edit, stage, or commit anything there.\n\nWHY THIS MATTERS: several target files exist at the SAME relative path in BOTH repos --\nplugins/saga/scripts/outcome_compat.py, plugins/saga/scripts/outcome.py,\nplugins/saga/scripts/outcome_dispatcher.py, plugins/saga/scripts/outcome_worktrees.py,\nand plugins/fleet-core/scripts/fleet_commons/lease_broker.py among them. Editing the\nwrong copy is a SILENT catastrophic failure: the edit applies cleanly, the suite passes,\nand nothing you were asked to change actually changed.\n\nMANDATORY GUARDS:\n  - The shell cwd RESETS between Bash calls in this environment. Begin every Bash\n    command with `cd /Users/jefcox/workspace/infiquetra/infiquetra-codex-plugins && ...` or use fully absolute paths.\n  - Use ABSOLUTE paths for every Read / Edit / Write call.\n  - Before your first edit, run:\n      git -C /Users/jefcox/workspace/infiquetra/infiquetra-codex-plugins rev-parse --show-toplevel\n    and confirm it prints the codex path. If it does not, STOP and report a\n    structural blocker instead of proceeding.\n  - When you need the upstream reference implementation, read it from /Users/jefcox/workspace/infiquetra/infiquetra-claude-plugins\n    at origin/main b464d090 -- read only, never write.\n\nSTAGING DISCIPLINE: this repo does NOT git-ignore `.claude/`, so the saga tick sits in\na committable path. NEVER run `git add -A` or `git add .` here. Stage explicit file\npaths only. Never stage `.claude/` or anything under it.\n\nBRANCH: `work/45-codex-refreeze-627-seam-cor3` is already checked out in the codex repo. Do not create or switch\nbranches. Commit your own unit's files with a conventional message ending in the\ntrailer `Claude-Session: https://claude.ai/code/session_014s77CGxva8ryGNQFCuYT77` and NO attribution footer of any kind. Do NOT push, do NOT open a\nPR, do NOT merge -- the driving session owns every outward mutation.\n\nRTK NOTE: this environment BLANKS `git diff | grep` pipelines. Write diffs to a file\nand parse them with python.\n\n--- unit prompt follows ---\n\nU3: Add refuse-mode admission to the codex fleet-core lease broker. Read docs/plans/2026-07-25-codex-refreeze-627-seam-and-cor3-worktree-authority-plan.md as your authoritative spec; U3, requirement R5, and KTD3/KTD5/KTD6 govern this unit.\n\nFIRST TASK, BEFORE ANY EDIT -- resolve KTD6. Codex carries TWO lease_broker.py files: plugins/saga/scripts/lease_broker.py (542 lines) and plugins/fleet-core/scripts/fleet_commons/lease_broker.py (4183 lines). Only the fleet-core copy carries _drop_superseded_resource_lease (2 refs) and _owner_state (3 refs); the saga copy carries neither. Determine what the saga copy IS -- vendored shim, stale duplicate, or deliberate narrow subset -- and state the finding explicitly. R5 targets the fleet-core copy. If the saga copy also needs the mode, that is a scope change to SURFACE, not to absorb silently.\n\nThen implement requirement R5 EXACTLY as the plan quotes it. READ THE REFERENCE IMPLEMENTATION -- Claude plugins/fleet-core/scripts/fleet_commons/lease_broker.py:2375-2425 -- and do NOT work from the issue comment's one-line summary, which is an incomplete paraphrase.\n\nThe signature gains `on_conflict: OnConflict = \"supersede\"`. The refusal is a THREE-PART conjunction: `prior_lease is not None` AND `not self._expired(prior_lease, monotonic=monotonic, boot_id=boot_id)` AND `self._owner_state(prior_lease) != \"dead\"`. Each arm has its own fall-through-to-supersede case -- a MISSING prior_lease supersedes; an EXPIRED prior is reclaimed in BOTH modes because the TTL+boot-id check precedes the liveness probe; a provably DEAD owner supersedes with no TTL wait. Only live and unknown on an UNEXPIRED prior refuse. A one-part reading that checks only _owner_state is wrong and will silently drop the expired-reclaim arm.\n\nR5a: the refusal raises LeaseConflictError carrying holder_owner_id=prior_lease.owner_id -- not a bare exception, not LeaseOwnershipError.\n\nR5b: refuse-mode is gated BELOW two existing precedence checks that must keep firing first and must not be reordered -- retained settlement authority and canonically-closed-requires-acquire_successor, both LeaseOwnershipError.\n\nDefault-mode call sites must be byte-identical in behavior. Claude's copy carries on_conflict x9, _owner_state x5, \"refuse\" x6; codex's carries 0, 3, 0.\n\nPORT THE FULL PUBLIC CONTRACT, NOT ONLY THE PRIVATE BRANCH. codex has ZERO occurrences of both OnConflict and LeaseConflictError, so adding only the private _drop_superseded_resource_lease branch leaves LeaseBroker.acquire_agent(on_conflict=\"refuse\") unsupported -- U4 would get TypeError and _lease_conflict_error_type() would have no typed conflict to recognize. Enumerate and port ALL of: the OnConflict = Literal[\"supersede\", \"refuse\"] alias (Claude :46), class LeaseConflictError(LeaseOwnershipError) with its holder_owner_id metadata (Claude :241), the public acquire_agent on_conflict parameter and default (Claude :2486+), its closed-value validation, the passthrough to the private helper, and subclass compatibility. Verify each against Claude b464d090 before porting.\n\nExtend plugins/fleet-core/tests/test_lease_broker.py with ALL ELEVEN scenarios U3 lists -- one per arm of the conjunction, plus both precedence gates, plus the two fall-through cases. The live-owner refusal must assert the prior lease's registry bytes are byte-for-byte untouched AND that holder_owner_id is populated, not merely that a refusal was returned. The expired-prior-is-reclaimed scenario is the one most likely to be missed.\n\nKTD5 is absolute: if you find a defect in the shared upstream mechanism, STOP and report it for upstream filing against infiquetra-claude-plugins. Never fix it codex-side.\n\nRETURN CONTRACT (all tiers): your FINAL message MUST be ONLY a single JSON object with the keys ktd6_determination, changed_files, public_api_ported, refuse_branch_impl, test_results, red_first_evidence, upstream_defect_found, stop_reason -- no prose, no markdown code fences, no YAML, and nothing before or after the JSON. The workflow gate parses this final message as JSON and FAILS the unit if it is not a JSON object carrying these keys. Emit it as your last action even if the work is only partial (fill each key with what you have plus a short note).",
  { label: "refuse-mode lease admission in the codex fleet-core broker", model: "opus", effort: "high", schema: {"additionalProperties": true, "properties": {"changed_files": {}, "ktd6_determination": {}, "public_api_ported": {}, "red_first_evidence": {}, "refuse_branch_impl": {}, "stop_reason": {}, "test_results": {}, "upstream_defect_found": {}}, "required": ["ktd6_determination", "changed_files", "public_api_ported", "refuse_branch_impl", "test_results", "red_first_evidence", "upstream_defect_found", "stop_reason"], "type": "object"} },
)
__gate(U3, { unitId: "U3", expectsOutput: true, returns: ["ktd6_determination", "changed_files", "public_api_ported", "refuse_branch_impl", "test_results", "red_first_evidence", "upstream_defect_found", "stop_reason"], cordProposal: "opus/high -> opus/xhigh (+1 effort rung)" })

// ---- U4: dispatcher transient-error arm and reconcile re-raise ordering ----
// depends_on: U3 (barrier)
const U4 = await agent(
  "WORKING DIRECTORY -- READ THIS FIRST, IT IS LOAD-BEARING.\n\nEvery relative path in this prompt belongs to the CODEX repo:\n  /Users/jefcox/workspace/infiquetra/infiquetra-codex-plugins\nThat is NOT this session's default working directory. The default cwd is\n  /Users/jefcox/workspace/infiquetra/infiquetra-claude-plugins\nwhich is the UPSTREAM CLAUDE repo. It is READ-ONLY REFERENCE for this work and it\ncarries the operator's uncommitted files. Never edit, stage, or commit anything there.\n\nWHY THIS MATTERS: several target files exist at the SAME relative path in BOTH repos --\nplugins/saga/scripts/outcome_compat.py, plugins/saga/scripts/outcome.py,\nplugins/saga/scripts/outcome_dispatcher.py, plugins/saga/scripts/outcome_worktrees.py,\nand plugins/fleet-core/scripts/fleet_commons/lease_broker.py among them. Editing the\nwrong copy is a SILENT catastrophic failure: the edit applies cleanly, the suite passes,\nand nothing you were asked to change actually changed.\n\nMANDATORY GUARDS:\n  - The shell cwd RESETS between Bash calls in this environment. Begin every Bash\n    command with `cd /Users/jefcox/workspace/infiquetra/infiquetra-codex-plugins && ...` or use fully absolute paths.\n  - Use ABSOLUTE paths for every Read / Edit / Write call.\n  - Before your first edit, run:\n      git -C /Users/jefcox/workspace/infiquetra/infiquetra-codex-plugins rev-parse --show-toplevel\n    and confirm it prints the codex path. If it does not, STOP and report a\n    structural blocker instead of proceeding.\n  - When you need the upstream reference implementation, read it from /Users/jefcox/workspace/infiquetra/infiquetra-claude-plugins\n    at origin/main b464d090 -- read only, never write.\n\nSTAGING DISCIPLINE: this repo does NOT git-ignore `.claude/`, so the saga tick sits in\na committable path. NEVER run `git add -A` or `git add .` here. Stage explicit file\npaths only. Never stage `.claude/` or anything under it.\n\nBRANCH: `work/45-codex-refreeze-627-seam-cor3` is already checked out in the codex repo. Do not create or switch\nbranches. Commit your own unit's files with a conventional message ending in the\ntrailer `Claude-Session: https://claude.ai/code/session_014s77CGxva8ryGNQFCuYT77` and NO attribution footer of any kind. Do NOT push, do NOT open a\nPR, do NOT merge -- the driving session owns every outward mutation.\n\nRTK NOTE: this environment BLANKS `git diff | grep` pipelines. Write diffs to a file\nand parse them with python.\n\n--- unit prompt follows ---\n\nU4: Add the typed transient-error arm to the codex dispatcher and fix the reconcile re-raise ordering. Read docs/plans/2026-07-25-codex-refreeze-627-seam-and-cor3-worktree-authority-plan.md as your authoritative spec; U4, requirements R6 and R7, and KTD3 govern this unit.\n\nTHERE IS NO LAYOUT DIVERGENCE HERE -- an earlier draft of the plan claimed one and was WRONG. _reconcile_once is defined in outcome.py in BOTH repos: Claude at :1097, codex at :1148. outcome_reconcile.py defines it in neither. Claude's reference implementation of this arm is outcome.py:1650-1690 -- read THAT. plugins/saga/scripts/outcome_dispatcher.py (542 lines) carries DispatcherError (11 refs, class at Claude :62) but neither DispatcherLeaseTransientError (Claude :66) nor _lease_conflict_error_type (Claude :79).\n\nAdd DispatcherLeaseTransientError(DispatcherError) raised at lease-conflict admission sites, classified through a shim-safe _lease_conflict_error_type() that DECLINES to transient when the shim fails to load. Make _reconcile_once re-raise any NON-transient DispatcherError BEFORE lease release and ledger writes, so permanent faults abort the tick loudly; only the transient subclass keeps halt-and-continue.\n\nPreserve codex's own outcome.dispatch.v2 intent and acknowledgement contract -- that string lives in plugins/saga/references/outcome-cross-runtime.md, plugins/saga/references/run-fact-ledger.md, and plugins/saga/tests/test_outcome_board_sync.py. This port must not disturb it, and test_outcome_board_sync.py must pass unmodified.\n\nCreate plugins/saga/tests/test_outcome_dispatcher.py (absent today) and extend plugins/saga/tests/test_outcome_reconcile.py. The load-bearing test is the ordering one, and requirement R7a is precise about WHICH lock: at the moment of the non-transient re-raise the per-subplot `dispatch-{sid}` STORE lock is STILL HELD (deliberate; self-heals after the 900 s DEFAULT_LEASE_TTL), the 300 s BROKER dispatch lease is ALREADY RELEASED by make_dispatcher's own finally before this arm runs, and the coordinator lock is released by the outer finally. Assert the store lock and the absence of any ledger write. DO NOT assert the broker lease is held -- that is false and would drive a wrong 'fix'. A test that only checks the exception type passes against an implementation that writes first, which is the defect R7 exists to prevent.\n\nAlso test R7b: the TRANSIENT path is not a bare continue. It must release the lock, append a reducer-VISIBLE (dispatch, halt) record paired to the intent's key -- built spread-first / literal-last so kind survives as \"dispatch\" for reduce_dispatch_ledger's halt arm and outcome_report._halted_subplots, preserving the receipt's own kind under receipt_kind -- and settle the attempt. Omitting the halt record is the #628 invisibility shape.\n\nTHERE IS NO U4/U5 DISPATCHER OVERLAP -- an earlier draft of this plan claimed one and was WRONG. codex ALREADY carries default_lease_authority() at plugins/saga/scripts/outcome_dispatcher.py:192. YOU (U4) ARE THE SOLE OWNER of every outcome_dispatcher.py edit in this chain; U5 is a read-only CONSUMER of that existing factory and will not touch the file. Do not defer any dispatcher change to U5 and do not report an overlap -- there is none to report.\n\nRETURN CONTRACT (all tiers): your FINAL message MUST be ONLY a single JSON object with the keys changed_files, transient_class_impl, classifier_impl, reraise_ordering_impl, test_results, red_first_evidence, board_sync_unmodified_pass, stop_reason -- no prose, no markdown code fences, no YAML, and nothing before or after the JSON. The workflow gate parses this final message as JSON and FAILS the unit if it is not a JSON object carrying these keys. Emit it as your last action even if the work is only partial (fill each key with what you have plus a short note).",
  { label: "dispatcher transient-error arm and reconcile re-raise ordering", model: "opus", effort: "high", schema: {"additionalProperties": true, "properties": {"board_sync_unmodified_pass": {}, "changed_files": {}, "classifier_impl": {}, "red_first_evidence": {}, "reraise_ordering_impl": {}, "stop_reason": {}, "test_results": {}, "transient_class_impl": {}}, "required": ["changed_files", "transient_class_impl", "classifier_impl", "reraise_ordering_impl", "test_results", "red_first_evidence", "board_sync_unmodified_pass", "stop_reason"], "type": "object"} },
)
__gate(U4, { unitId: "U4", expectsOutput: true, returns: ["changed_files", "transient_class_impl", "classifier_impl", "reraise_ordering_impl", "test_results", "red_first_evidence", "board_sync_unmodified_pass", "stop_reason"], cordProposal: "opus/high -> opus/xhigh (+1 effort rung)" })

// ---- U5: COR3 worktree lease-authority subsystem port ----
// depends_on: U4 (barrier)
const U5 = await agent(
  "WORKING DIRECTORY -- READ THIS FIRST, IT IS LOAD-BEARING.\n\nEvery relative path in this prompt belongs to the CODEX repo:\n  /Users/jefcox/workspace/infiquetra/infiquetra-codex-plugins\nThat is NOT this session's default working directory. The default cwd is\n  /Users/jefcox/workspace/infiquetra/infiquetra-claude-plugins\nwhich is the UPSTREAM CLAUDE repo. It is READ-ONLY REFERENCE for this work and it\ncarries the operator's uncommitted files. Never edit, stage, or commit anything there.\n\nWHY THIS MATTERS: several target files exist at the SAME relative path in BOTH repos --\nplugins/saga/scripts/outcome_compat.py, plugins/saga/scripts/outcome.py,\nplugins/saga/scripts/outcome_dispatcher.py, plugins/saga/scripts/outcome_worktrees.py,\nand plugins/fleet-core/scripts/fleet_commons/lease_broker.py among them. Editing the\nwrong copy is a SILENT catastrophic failure: the edit applies cleanly, the suite passes,\nand nothing you were asked to change actually changed.\n\nMANDATORY GUARDS:\n  - The shell cwd RESETS between Bash calls in this environment. Begin every Bash\n    command with `cd /Users/jefcox/workspace/infiquetra/infiquetra-codex-plugins && ...` or use fully absolute paths.\n  - Use ABSOLUTE paths for every Read / Edit / Write call.\n  - Before your first edit, run:\n      git -C /Users/jefcox/workspace/infiquetra/infiquetra-codex-plugins rev-parse --show-toplevel\n    and confirm it prints the codex path. If it does not, STOP and report a\n    structural blocker instead of proceeding.\n  - When you need the upstream reference implementation, read it from /Users/jefcox/workspace/infiquetra/infiquetra-claude-plugins\n    at origin/main b464d090 -- read only, never write.\n\nSTAGING DISCIPLINE: this repo does NOT git-ignore `.claude/`, so the saga tick sits in\na committable path. NEVER run `git add -A` or `git add .` here. Stage explicit file\npaths only. Never stage `.claude/` or anything under it.\n\nBRANCH: `work/45-codex-refreeze-627-seam-cor3` is already checked out in the codex repo. Do not create or switch\nbranches. Commit your own unit's files with a conventional message ending in the\ntrailer `Claude-Session: https://claude.ai/code/session_014s77CGxva8ryGNQFCuYT77` and NO attribution footer of any kind. Do NOT push, do NOT open a\nPR, do NOT merge -- the driving session owns every outward mutation.\n\nRTK NOTE: this environment BLANKS `git diff | grep` pipelines. Write diffs to a file\nand parse them with python.\n\n--- unit prompt follows ---\n\nU5: Port the worktree lease-authority subsystem into codex. Read docs/plans/2026-07-25-codex-refreeze-627-seam-and-cor3-worktree-authority-plan.md as your authoritative spec; U5, requirement R8, and KTD3/KTD4/KTD5 govern this unit.\n\nThis is a SUBSYSTEM PORT, not parameter threading -- KTD4 reclassifies it from the issue's framing. Codex plugins/saga/scripts/outcome_worktrees.py is 505 lines and has ZERO lease_authority references. It lacks all nine of: WorktreeAuthorityError, ReapPreflight, read_registry_strict, _lease_binding, prevalidate_reap_authority, _arm_worktree, stale_worktree_debits, _reap_prevalidated, reconcile_worktree_leases. Its reap_worktree at :254 has signature (store, subplot_id, ops, *, at=\"\") -- no lease_authority, no release_authority. Claude's equivalent module is 980 lines.\n\nSource of truth is /Users/jefcox/workspace/infiquetra/infiquetra-claude-plugins at origin/main b464d090. The full Claude surface is 55 lease_authority lines across three modules: outcome_worktrees.py (46), outcome.py (6), outcome_dispatcher.py (3, default_lease_authority() at :255); outcome_reconcile.py carries none.\n\nTHE outcome.py SEAM IS A PROCESSOR FACTORY, NOT A VERB PARAMETER -- an earlier draft of the plan said 'thread it into prune and advance', which is WRONG and would send you looking for parameters that do not exist. Claude's 6 lines sit in production_worktree_processor (def at :2256, parameter at :2262/:2273, nested processor closure at :2284/:2292). main() (:2359) wires authority in TWO places: lease_authority=default_lease_authority() into make_dispatcher at :2689, and worktree_processor=production_worktree_processor(root) at :2693. The advance (:2426) and prune (:2567) subparsers CONSUME those wired processors and take no authority argument of their own. Port the factory and the wiring.\n\nDO NOT byte-copy. Per KTD3 these are codex-native modules that deliberately diverge; port the mechanism and adapt it to codex's structure.\n\nCreate plugins/saga/tests/test_outcome_worktrees.py (absent today) with ALL ELEVEN scenarios U5 lists (the last five are failure modes -- second reap idempotency, malformed receipt, registry mutated after preflight, removal failure, authority-release failure -- and are NOT optional). The compatibility floor -- reap_worktree with lease_authority=None preserving today's behavior exactly -- is a characterization test that must pass BOTH before and after. prevalidate_reap_authority must be shown to refuse BEFORE any filesystem mutation, asserted on ordering rather than on the raise alone.\n\nTHERE IS NO U4/U5 DISPATCHER OVERLAP -- an earlier draft claimed one and was WRONG. codex ALREADY has default_lease_authority() at outcome_dispatcher.py:192, so U5 adds nothing to that file. U4 is the SOLE owner of outcome_dispatcher.py; U5 is a read-only CONSUMER of the existing authority factory and must not edit it.\n\nKTD5 is absolute: a new defect in the shared mechanism is an upstream filing and a stop, never a codex-side fix.\n\nRETURN CONTRACT (all tiers): your FINAL message MUST be ONLY a single JSON object with the keys ported_symbols, codex_adaptations, changed_files, test_results, red_first_evidence, compatibility_floor_result, stop_reason -- no prose, no markdown code fences, no YAML, and nothing before or after the JSON. The workflow gate parses this final message as JSON and FAILS the unit if it is not a JSON object carrying these keys. Emit it as your last action even if the work is only partial (fill each key with what you have plus a short note).",
  { label: "COR3 worktree lease-authority subsystem port", model: "opus", effort: "high", schema: {"additionalProperties": true, "properties": {"changed_files": {}, "codex_adaptations": {}, "compatibility_floor_result": {}, "ported_symbols": {}, "red_first_evidence": {}, "stop_reason": {}, "test_results": {}}, "required": ["ported_symbols", "codex_adaptations", "changed_files", "test_results", "red_first_evidence", "compatibility_floor_result", "stop_reason"], "type": "object"} },
)
__gate(U5, { unitId: "U5", expectsOutput: true, returns: ["ported_symbols", "codex_adaptations", "changed_files", "test_results", "red_first_evidence", "compatibility_floor_result", "stop_reason"], cordProposal: "opus/high -> opus/xhigh (+1 effort rung)" })

// ---- U6: release surfaces, engineering journal, and the cross-runtime acceptance leg ----
// depends_on: U5 (barrier)
const U6 = await agent(
  "WORKING DIRECTORY -- READ THIS FIRST, IT IS LOAD-BEARING.\n\nEvery relative path in this prompt belongs to the CODEX repo:\n  /Users/jefcox/workspace/infiquetra/infiquetra-codex-plugins\nThat is NOT this session's default working directory. The default cwd is\n  /Users/jefcox/workspace/infiquetra/infiquetra-claude-plugins\nwhich is the UPSTREAM CLAUDE repo. It is READ-ONLY REFERENCE for this work and it\ncarries the operator's uncommitted files. Never edit, stage, or commit anything there.\n\nWHY THIS MATTERS: several target files exist at the SAME relative path in BOTH repos --\nplugins/saga/scripts/outcome_compat.py, plugins/saga/scripts/outcome.py,\nplugins/saga/scripts/outcome_dispatcher.py, plugins/saga/scripts/outcome_worktrees.py,\nand plugins/fleet-core/scripts/fleet_commons/lease_broker.py among them. Editing the\nwrong copy is a SILENT catastrophic failure: the edit applies cleanly, the suite passes,\nand nothing you were asked to change actually changed.\n\nMANDATORY GUARDS:\n  - The shell cwd RESETS between Bash calls in this environment. Begin every Bash\n    command with `cd /Users/jefcox/workspace/infiquetra/infiquetra-codex-plugins && ...` or use fully absolute paths.\n  - Use ABSOLUTE paths for every Read / Edit / Write call.\n  - Before your first edit, run:\n      git -C /Users/jefcox/workspace/infiquetra/infiquetra-codex-plugins rev-parse --show-toplevel\n    and confirm it prints the codex path. If it does not, STOP and report a\n    structural blocker instead of proceeding.\n  - When you need the upstream reference implementation, read it from /Users/jefcox/workspace/infiquetra/infiquetra-claude-plugins\n    at origin/main b464d090 -- read only, never write.\n\nSTAGING DISCIPLINE: this repo does NOT git-ignore `.claude/`, so the saga tick sits in\na committable path. NEVER run `git add -A` or `git add .` here. Stage explicit file\npaths only. Never stage `.claude/` or anything under it.\n\nBRANCH: `work/45-codex-refreeze-627-seam-cor3` is already checked out in the codex repo. Do not create or switch\nbranches. Commit your own unit's files with a conventional message ending in the\ntrailer `Claude-Session: https://claude.ai/code/session_014s77CGxva8ryGNQFCuYT77` and NO attribution footer of any kind. Do NOT push, do NOT open a\nPR, do NOT merge -- the driving session owns every outward mutation.\n\nRTK NOTE: this environment BLANKS `git diff | grep` pipelines. Write diffs to a file\nand parse them with python.\n\n--- unit prompt follows ---\n\nU6: Move release surfaces and run the cross-runtime acceptance leg. Read docs/plans/2026-07-25-codex-refreeze-627-seam-and-cor3-worktree-authority-plan.md as your authoritative spec; U6 and requirements R9 and R11 govern this unit.\n\nFor every plugin whose files changed in U1-U5, move its release surfaces in the same PR. THE PATHS ARE VERIFIED: plugin manifests are plugins/<plugin>/.codex-plugin/plugin.json (NOT .claude-plugin/), and the marketplace is .agents/plugins/marketplace.json (NOT .claude-plugin/marketplace.json, which DOES NOT EXIST in this repo -- never create it). Plus each plugin's CHANGELOG.md. THIS IS ONE PR (KTD2) -- an earlier draft proposed three and was rejected: /work has one PR-ready boundary per execution contract, and the acceptance harness appears in NONE of the four .github/workflows/ files, so an early port-digest green unblocks nothing. Current versions are saga 0.79.0+... and fleet-core 0.11.0+...; both carry behavior here so both bump. Choose the exact target versions when the diff is final and move manifest + marketplace + CHANGELOG together as one release unit. Add the KTD entries to docs/engineering-journal/DECISIONS.md and any non-obvious mechanism to LEARNINGS.md.\n\nThen run the cross-runtime acceptance harness, tools/run_cross_runtime_outcome_acceptance.py in the infiquetra-claude-plugins clone, revision-pinned on both runtimes. Use ABSOLUTE pin paths -- the shell cwd resets between commands in this environment -- and export TMPDIR=$(mktemp -d) first, since failing runs retain their workdirs.\n\nREPORT THE LEG MATRIX VERBATIM AND DO NOT OVERCLAIM. contract_digests / port-digest has been halting BY DESIGN since the #627 merge and U2's outcome_compat.py re-freeze is what addresses it (contract_digests compares ONLY that file, after normalizing RUNTIME_LABEL). SEPARATELY, #628 (cross-runtime double dispatch) was already documenting 12/14 with race-codex-first and race-simultaneous red. This plan does NOT claim to change those two legs. A leg that stays red is documented truth per R11 -- never work around it, never silently reframe it.\n\nALSO IN THIS UNIT -- the inventory rebuild and the cutover gate:\n\nREBUILD docs/validation/saga-family-target-inventory.json after the release surfaces move, and assert its recomputed sha256 against what the acceptance bundle records. This comparison belongs HERE, not in U1: the bundle does not exist until the acceptance leg has run. NOTE THE LIMIT -- the harness only RECORDS this digest. contract_digests (tools/run_cross_runtime_outcome_acceptance.py:262-267) calls _sha256_file and returns the value without comparing it; the only port-digest raise is the normalized outcome_compat.py comparison at :256-261. A stale inventory does NOT fail the harness. The freshness proof is this repo's own generated-file check, not the harness.\n\nCUTOVER GATE: run `python3 scripts/port_contract.py validate --stage unit --unit U<n>` per implementation unit, then `--stage cutover` on the U1 contract. Cutover additionally requires every non-deferred row at state=verified WITH evidence_refs, current rationales on any surviving defer/reject, and the release_evidence keys populated -- _validate_cutover_release_proof (port_contract.py:1405) rejects a null. Advance each claimed row classified -> implemented -> verified as its unit lands; do not batch-flip them at the end without evidence.\n\nRETURN CONTRACT (all tiers): your FINAL message MUST be ONLY a single JSON object with the keys release_surface_diffs, versions_chosen, journal_entries, acceptance_leg_matrix, legs_changed, legs_still_red, inventory_rebuilt_sha256, unit_gate_results, cutover_gate_result, stop_reason -- no prose, no markdown code fences, no YAML, and nothing before or after the JSON. The workflow gate parses this final message as JSON and FAILS the unit if it is not a JSON object carrying these keys. Emit it as your last action even if the work is only partial (fill each key with what you have plus a short note).",
  { label: "release surfaces, engineering journal, and the cross-runtime acceptance leg", model: "sonnet", effort: "medium", schema: {"additionalProperties": true, "properties": {"acceptance_leg_matrix": {}, "cutover_gate_result": {}, "inventory_rebuilt_sha256": {}, "journal_entries": {}, "legs_changed": {}, "legs_still_red": {}, "release_surface_diffs": {}, "stop_reason": {}, "unit_gate_results": {}, "versions_chosen": {}}, "required": ["release_surface_diffs", "versions_chosen", "journal_entries", "acceptance_leg_matrix", "legs_changed", "legs_still_red", "inventory_rebuilt_sha256", "unit_gate_results", "cutover_gate_result", "stop_reason"], "type": "object"} },
)
__gate(U6, { unitId: "U6", expectsOutput: true, returns: ["release_surface_diffs", "versions_chosen", "journal_entries", "acceptance_leg_matrix", "legs_changed", "legs_still_red", "inventory_rebuilt_sha256", "unit_gate_results", "cutover_gate_result", "stop_reason"], cordProposal: "sonnet/medium -> sonnet/high (+1 effort rung)" })

if (__pulledCords.length > 0) {
  throw new Error(`pull-cord (#364): ${__pulledCords.length} unit(s) self-reported out of depth -- ` +
    __pulledCords.map((c) => `${c.unit}: ${c.reason}` + (c.proposal ? ` (propose ${c.proposal})` : ' (no legal climb: top of ladder or session ceiling -- HALT)')).join('; ') +
    '. ONE batched escalation ask -- confirm climbs via /tier patch and re-emit.')
}
