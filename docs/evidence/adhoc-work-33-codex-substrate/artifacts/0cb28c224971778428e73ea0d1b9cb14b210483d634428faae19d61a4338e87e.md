---
verdict: ship-with-deferred
health_score: 100
---

# QA report — lease-safe substrate port (#33, U1–U4 pre-release gate)

- **Target**: branch `work/33-codex-substrate` at **HEAD `6751ac53cd3418ee14731f3cfe68dd89e7618968`** (code SHA `b52280ae`; REVIEWED_SHA `acc0d1da` + review-artifact commit). Pre-merge; destination merge via the U5 atomic PR.
- **Tier**: Standard (critical + high + medium block). Criteria frozen pre-check (`docs/evidence/adhoc-work-33-codex-substrate/criteria-qa-6751ac53….json`, hash `d5d11c82`).
- **Scope**: behavior, security, docs, config (diff-aware map over 37 files, +17727/−40, 12 commits). No UI surface — browser MCP check is a graceful no-op. U5 (release surfaces, isolated install, fresh-session readback, rollback) is intentionally unbuilt: its proof is U5's own acceptance evidence, out of this gate's scope.
- **Baseline**: none (first QA run for this thread) — delta null.

## Health score

| Class | Score | Result |
|---|---|---|
| behavior | 100 | PASS |
| security | 100 | PASS |
| docs | 100 | PASS |
| config | 97 | PASS (1 low, non-blocking) |
| **Overall** | **100** | **ship-with-deferred** |

## Checks run and evidence

**behavior** — full codex suite **2447 passed** at HEAD (`PYTHONPATH=. uv run pytest -q -p no:cacheprovider`). Live end-to-end acceptance probe in a scratch root (outside the repo): broker `acquire → renew → fenced-intruder release refused with zero registry-byte mutation → release → empty registry`, and settlement `manifest → spawn → duplicate spawn refused → delivered settle (evidence-bound) → report(halt_required=False) → zero open positions`; all writes confined to the scratch root (registry.json/.lock, run-facts.jsonl/.lock only). Cross-runtime conformance digests pinned in-suite (broker `f60fd482…`, ledger `34804e26…`) — root- and machine-independent by construction.

**security** — no new probes warranted beyond the converged evidence: the ceremony security lens (opus, r1) and the code-review security lens both CLEAN with hermetic-fixture proof (real-HOME before/after listings), no secrets/PII in added lines, restrictive modes (0o600/0o700), fenced authority negative oracles byte-exact. Accepted residual: audit_store ancestor-dir validation deferred while the module is dormant (zero in-tree callers; tracked for the first live wiring).

**docs** — evidence-chain integrity: per-port gate test recomputes every evidence `artifact_sha256` against current bytes (13/13 green); manifest plan/doc-review byte-bindings re-verified MATCH this session; ceremony record (`docs/validation/lease-safe-substrate-ceremony.md`) and code-review artifact (`docs/code-reviews/2026-07-19-lease-safe-substrate-code-review.md`) committed; manifest cross-references all resolve (maintainability lens walk).

**config** — `validate_codex_plugins.py` green; `build_legacy_workflow_inventory.py --check` exit 0; `git diff --check` clean; per-port gate 13/13. The plan Verification section's `port_contract.py validate --stage …` lines are satisfied by the per-port pytest gate equivalent: the CLI is hard-pinned to the 2026-07-11 external-advisory port (its port_id, frozen refs, 3+7 row counts, frozen digests — `scripts/port_contract.py:41-53,975-1030`), so no later port can pass it; later ports gate via their own pytest file (U1 decision, mission-control 2100/2101 precedent). One finding below.

## Findings

### QA-1 — low / config — manifest `refresh_changes` retained after reconciliation (deferred)

`docs/portability/ports/2026-07-19-lease-safe-substrate.json` carries one `refresh_changes` entry (the U3 run_ledger planned-test path refinement) that is already reconciled into its row. The contract treats the field as a drain-queue, not a history log: `port_contract.py:1196-1197` requires it empty for classification, both prior merged port manifests carry it empty, and a future `refresh` run overwrites the field wholesale (`port_contract.py:1451`), so the retained entry is not durable history anyway (git history and the row rationale preserve it). No gate impact: the CLI stage validate is unreachable for this port (pinned to the 2026-07-11 port), and the per-port pytest gate does not read the field.
**Repro**: `uv run python scripts/port_contract.py validate --manifest docs/portability/ports/2026-07-19-lease-safe-substrate.json --stage classification` → `refresh_changes must be empty…` among the 13 expected pin errors.
**Deferred to**: U5's manifest touch (release_evidence update) — drain to `[]` in the same commit.

## Recommended regression tests (recommend only)

- When U5 adds cutover assertions to the per-port gate, include `assert manifest["refresh_changes"] == []` so the drain-contract is pinned for this and future ports.

## Verdict derivation

Standard tier blocks critical + high + medium. Findings: one low (QA-1, deferred with repro). No class has a blocking finding → all four classes PASS → **ship-with-deferred**. The substrate (U1–U4) is ready for the U5 release unit; U5's own gates (release surfaces, isolated install, fresh-session readback, exact rollback, cutover) remain ahead of the PR.

## Saga

No work-thread saga exists in this repository's store for #33 (leaf tracked as `leaf-lease-safe-runtime-continuity-codex-substrate` in the outcome ledger, claude-plugins side) — saga tick skipped; never minted from /qa.
